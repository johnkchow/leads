<%= form_for(@email_template) do |f| %>
  <% if @email_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@email_template.errors.count, "error") %> prohibited this email_template from being saved:</h2>

      <ul>
      <% @email_template.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :subject %><br />
    <%= f.text_area :subject, :class => "span6", :rows => 4 %>
  </div>
  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_area :text, :class => "span6" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
